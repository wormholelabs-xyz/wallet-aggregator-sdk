# Security: Only install packages published at least 24 hours ago
# Protects against supply chain attacks by waiting for community to detect malware
minimum-release-age=1440

# Exclude internal/trusted packages from the 24h delay if needed
minimum-release-age-exclude=@wormhole-foundation/*,@wormhole-labs/*
